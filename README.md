# README

On-Together DnD mod:

- DnDUtil :: Mod to help players play chat-based DnD

## Get Started

Run `dotnet build -v d` to install locally 

Run `dotnet build -v d -c Release` to generate Thunderstore Zip
